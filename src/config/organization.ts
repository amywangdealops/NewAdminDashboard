// Organization configuration
// Change this to customize for different enterprise organizations
export const organizationConfig = {
  name: "Harvey", // Change to "Airwallex", "Harvey", or any other org name
  version: "2.0", // Optional version identifier
  displayName: "Harvey 2.0", // Full display name shown in sidebar
};

